enum Gender { male, female }

enum WeightGoal {
  weightLoss,
  weightGain,
  maintenance,
}

enum ActivityLevel {
  sedentary,
  lightlyActive,
  moderatelyActive,
  veryActive,
  extraActive,
}

enum PushPullType { push, pull, legs, other }

enum EquipmentType {
  barbell,
  dumbbell,
  machine,
  bodyweight,
  cable,
  kettlebell,
  band,
  other,
}

enum MuscleGroup {
  chest,
  back,
  shoulders,
  arms,
  legs,
  core,
  fullBody,
  other,
}

enum SplitType {
  push,
  pull,
  legs,
  fullBody,
  upper,
  lower,
  other,
}
