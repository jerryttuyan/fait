enum Gender { male, female }

enum WeightGoal {
  weightLoss,
  weightGain,
  maintenance,
}

enum ActivityLevel {
  sedentary,
  lightlyActive,
  moderatelyActive,
  veryActive,
  extraActive,
}

enum PushPullType { push, pull, legs, fullBody, cardio, other }

enum EquipmentType {
  barbell,
  dumbbell,
  machine,
  bodyweight,
  cable,
  kettlebell,
  band,
  other,
}

enum MuscleGroup {
  abs,
  back,
  biceps,
  chest,
  glutes,
  hamstrings,
  quadriceps,
  shoulders,
  triceps,
  lowerBack,
  cardio,
}

enum SplitType {
  push,
  pull,
  legs,
  fullBody,
  upper,
  lower,
  other,
}
